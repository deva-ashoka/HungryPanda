1. Add all the .jar files present in the ‘lib’ folder into intelliJ (Project Structure —> Modules —> Dependencies —> add .jars)
2. Setup Tomcat server
3. Fix all the Artifacts/Facets issues, if any
3. Setup MongoDB on your PC
4. Run the WebApp